#include <bits/stdc++.h>
using namespace std;

void funcion (int n){
    for (int i = 1; i < n; i++)
    {
        int x = 
    }
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int testcases;
    cin >> testcases;
    vector <int> arre (n);
    while(testcases--) {
      int n;
      cin >> n;
      
        
    }

    return 0;
}


for (int i = 1; i < s.size(); i++)
        {
            int j=1;
            while (arre[i] % j == 0){
            if(arre[i] % j == 0){
                s2.insert(i);
                j++;
                //f.push_back(i);
                //arre[i] /= i;
            }
            else
            break;
            }
        }
        if (n > 1)
                s.insert(n);

        